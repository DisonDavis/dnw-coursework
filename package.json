{
  "name": "event-manager",
  "version": "1.0.0",
  "scripts": {
    "build-db": "sqlite3 database.db < db_schema.sql",
    "start": "node index.js",
    "clean-db": "rm -f database.db"
  },
  "dependencies": {
    "dotenv": "^17.0.1",
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "express-session": "^1.18.1",
    "sqlite3": "^5.1.2"
  }
}
